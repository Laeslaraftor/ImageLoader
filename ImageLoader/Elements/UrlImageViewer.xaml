<UserControl x:Class="ImageLoader.Elements.UrlImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageLoader.Elements" 
             xmlns:viewmodels="clr-namespace:ImageLoader.Elements.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:UrlImageViewerViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Margin" Value="4"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Margin" Value="4"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="38"/>
            <RowDefinition Height="38"/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>
        <TextBlock Foreground="IndianRed"
                   Text="{Binding ExceptionMessage}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DownloadFailed}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DownloadFailed}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <Image Height="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Parent.ActualWidth}"
               Source="{Binding Image}"
               Stretch="Uniform"/>
        <TextBox Grid.Row="1"
                 Text="{Binding Url, Mode=TwoWay}"/>
        <Button Grid.Row="2" Content="Start"
                Command="{Binding DownloadCommand}"/>
        <Button Grid.Row="3" Content="Stop"
                Command="{Binding CancelCommand}"/>
    </Grid>
</UserControl>
